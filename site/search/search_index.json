{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Welcome to Risansym","text":"<p>Risansym is a modern, strict, and performant discrete event simulator built for designing and analyzing distributed systems algorithms.</p> <p>It offers a robust Python core powered by <code>Pydantic</code> and a beautiful, high-performance web visualizer built with <code>React</code> and <code>Next.js</code>.</p>"},{"location":"#why-risansym","title":"Why Risansym?","text":"<ul> <li>Strict &amp; Type-Safe: Built with Python's modern typing system and validated using <code>Pydantic</code>.</li> <li>Decoupled Architecture: The core simulation engine is completely independent of the visualizer. It generates rich JSON traces that can be analyzed anywhere.</li> <li>Visual Feedback: Load your traces into the web visualizer to watch your algorithm unfold event by event.</li> <li>Educational &amp; Professional: Whether you're learning about logical clocks (e.g. Chandy-Lamport) or prototyping a complex network protocol, Risansym provides the perfect environment.</li> </ul>"},{"location":"#next-steps","title":"Next Steps","text":"<ul> <li>Jump into the Getting Started guide to install the library.</li> <li>Learn how to build your first protocol in the Writing Algorithms tutorial.</li> <li>Explore the API Reference for detailed documentation on the framework's classes.</li> </ul>"},{"location":"architecture/","title":"System Architecture","text":"<p>Risansym operates as two completely decoupled systems: a Python Engine and a Web Visualizer. They communicate strictly through serialized JSON trace files.</p>"},{"location":"architecture/#the-python-engine-core","title":"The Python Engine (Core)","text":"<p>The core is a classic Discrete Event Simulator (DES) built entirely in Python.</p>"},{"location":"architecture/#simulation-loop","title":"Simulation Loop","text":"<p>At the heart of the engine is the <code>Simulator</code> (or Agenda). It maintains a global clock and a Priority Queue (Min-Heap). When an <code>Event</code> is transmitted, it is pushed into the priority queue, sorted primarily by its <code>time</code> attribute. The main loop pops the event with the lowest timestamp, advances the global clock to that time, and invokes the <code>receive</code> callback on the target <code>Process</code>.</p>"},{"location":"architecture/#processes-and-models","title":"Processes and Models","text":"<p>A <code>Process</code> represents a physical node wrapper in the topology graph. It holds metadata (like its <code>id</code> and <code>neighbors</code>). A <code>Model</code> is the user-defined algorithm behavior. When a user creates a simulation, they map instances of their <code>Model</code> to <code>Process</code> nodes.</p>"},{"location":"architecture/#the-web-visualizer-react","title":"The Web Visualizer (React)","text":"<p>The visualizer is built using React and Next.js. It loads the JSON trace file into a statically typed format using <code>Zod</code>.</p>"},{"location":"architecture/#rendering","title":"Rendering","text":"<p>The timeline is drawn using SVG elements.  The visualizer maps time to the X-axis and nodes to the Y-axis (lifelines).</p>"},{"location":"architecture/#animation","title":"Animation","text":"<p>The playback engine uses <code>requestAnimationFrame</code> to compute a smooth delta-time interpolation.  Pending messages are rendered dynamically. Since network transmission occurs over a period of time, the message is drawn as a smooth B\u00e9zier curve using the De Casteljau algorithm between the source and target coordinates.</p>"},{"location":"api/event/","title":"Event","text":""},{"location":"api/event/#risansym.Event","title":"<code>risansym.Event</code>  <code>dataclass</code>","text":"<p>Encapsulates the information exchanged between active processes in the simulation.</p> Source code in <code>core/src/risansym/event.py</code> <pre><code>@dataclass(order=True, slots=True, frozen=True)\nclass Event:\n    \"\"\"Encapsulates the information exchanged between active processes in the simulation.\"\"\"\n\n    time: float\n    # field(compare=False) prevents tie-breaking by name/IDs when times are equal\n    name: str = field(compare=False)\n    target: int = field(compare=False)\n    source: int = field(compare=False)\n    payload: JsonPayload = field(default_factory=dict, compare=False)\n\n    def __repr__(self) -&gt; str:\n        return f\"Event(t={self.time}, '{self.name}' {self.source}\u2192{self.target})\"\n</code></pre>"},{"location":"api/model/","title":"Model","text":""},{"location":"api/model/#risansym.Model","title":"<code>risansym.Model</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract interface (contract) for distributed algorithms.</p> <p>Subclasses must implement <code>init()</code> and <code>receive()</code> to define the node's state-machine logic.</p> <p>Attributes:</p> Name Type Description <code>clock</code> <code>float</code> <p>Current simulation time as seen by this node.</p> <code>sink</code> <code>MessageSink | None</code> <p>Back-reference to the hosting environment (set during binding).</p> <code>neighbors</code> <code>list[int]</code> <p>List of adjacent node IDs in the topology graph.</p> <code>id</code> <code>int</code> <p>Unique identifier of the node this model is bound to.</p> Source code in <code>core/src/risansym/model.py</code> <pre><code>class Model(ABC):\n    \"\"\"Abstract interface (contract) for distributed algorithms.\n\n    Subclasses must implement ``init()`` and ``receive()`` to define\n    the node's state-machine logic.\n\n    Attributes:\n        clock: Current simulation time as seen by this node.\n        sink: Back-reference to the hosting environment (set during binding).\n        neighbors: List of adjacent node IDs in the topology graph.\n        id: Unique identifier of the node this model is bound to.\n    \"\"\"\n\n    def __init__(self) -&gt; None:\n        self.clock: float = 0.0\n        self.sink: MessageSink | None = None\n        self.neighbors: list[int] = []\n        self.id: int = 0\n\n    def __repr__(self) -&gt; str:\n        return f\"&lt;{self.__class__.__name__}(id={self.id}, clock={self.clock})&gt;\"\n\n    # ------------------------------------------------------------------\n    # Internal setters \u2014 called by the framework, not by user algorithms\n    # ------------------------------------------------------------------\n\n    def set_time(self, time: float) -&gt; None:\n        \"\"\"Advance the node's local clock (called by the framework).\"\"\"\n        self.clock = time\n\n    def set_sink(self, sink: MessageSink, neighbors: list[int], node_id: int) -&gt; None:\n        \"\"\"Bind this model to its host environment and topology context (called by the framework).\"\"\"\n        self.sink = sink\n        self.neighbors = neighbors\n        self.id = node_id\n\n    # ------------------------------------------------------------------\n    # Public API for user algorithms\n    # ------------------------------------------------------------------\n\n    def transmit(self, event: Event) -&gt; None:\n        \"\"\"Schedule a message transmission to another node.\"\"\"\n        if self.sink is None:\n            raise RuntimeError(\n                f\"Model(id={self.id}) cannot transmit: not bound to a Process. \"\n                \"Ensure Simulation.initialize_all() has been called.\"\n            )\n        self.sink.transmit(event)\n\n    def log(self, message: str) -&gt; None:\n        \"\"\"Record an application-level log event in the trace.\n\n        Use this instead of ``print()`` so that log entries appear in the\n        trace output and are visible in the web visualizer.\n        \"\"\"\n        if self.sink is None:\n            raise RuntimeError(\n                f\"Model(id={self.id}) cannot log: not bound to a Process. \"\n                \"Ensure Simulation.initialize_all() has been called.\"\n            )\n        self.sink.log(message)\n\n    def get_state(self) -&gt; dict[str, Any]:\n        \"\"\"Return a snapshot of the node's internal state.\n\n        Override in subclasses to expose algorithm-specific state that\n        will be captured in the trace after each event is processed.\n        \"\"\"\n        return {}\n\n    @abstractmethod\n    def init(self) -&gt; None:\n        \"\"\"Initialize local state (implemented by the subclass).\"\"\"\n\n    @abstractmethod\n    def receive(self, event: Event) -&gt; None:\n        \"\"\"State-machine transition logic (implemented by the subclass).\"\"\"\n</code></pre>"},{"location":"api/model/#risansym.Model.get_state","title":"<code>get_state()</code>","text":"<p>Return a snapshot of the node's internal state.</p> <p>Override in subclasses to expose algorithm-specific state that will be captured in the trace after each event is processed.</p> Source code in <code>core/src/risansym/model.py</code> <pre><code>def get_state(self) -&gt; dict[str, Any]:\n    \"\"\"Return a snapshot of the node's internal state.\n\n    Override in subclasses to expose algorithm-specific state that\n    will be captured in the trace after each event is processed.\n    \"\"\"\n    return {}\n</code></pre>"},{"location":"api/model/#risansym.Model.init","title":"<code>init()</code>  <code>abstractmethod</code>","text":"<p>Initialize local state (implemented by the subclass).</p> Source code in <code>core/src/risansym/model.py</code> <pre><code>@abstractmethod\ndef init(self) -&gt; None:\n    \"\"\"Initialize local state (implemented by the subclass).\"\"\"\n</code></pre>"},{"location":"api/model/#risansym.Model.log","title":"<code>log(message)</code>","text":"<p>Record an application-level log event in the trace.</p> <p>Use this instead of <code>print()</code> so that log entries appear in the trace output and are visible in the web visualizer.</p> Source code in <code>core/src/risansym/model.py</code> <pre><code>def log(self, message: str) -&gt; None:\n    \"\"\"Record an application-level log event in the trace.\n\n    Use this instead of ``print()`` so that log entries appear in the\n    trace output and are visible in the web visualizer.\n    \"\"\"\n    if self.sink is None:\n        raise RuntimeError(\n            f\"Model(id={self.id}) cannot log: not bound to a Process. \"\n            \"Ensure Simulation.initialize_all() has been called.\"\n        )\n    self.sink.log(message)\n</code></pre>"},{"location":"api/model/#risansym.Model.receive","title":"<code>receive(event)</code>  <code>abstractmethod</code>","text":"<p>State-machine transition logic (implemented by the subclass).</p> Source code in <code>core/src/risansym/model.py</code> <pre><code>@abstractmethod\ndef receive(self, event: Event) -&gt; None:\n    \"\"\"State-machine transition logic (implemented by the subclass).\"\"\"\n</code></pre>"},{"location":"api/model/#risansym.Model.set_sink","title":"<code>set_sink(sink, neighbors, node_id)</code>","text":"<p>Bind this model to its host environment and topology context (called by the framework).</p> Source code in <code>core/src/risansym/model.py</code> <pre><code>def set_sink(self, sink: MessageSink, neighbors: list[int], node_id: int) -&gt; None:\n    \"\"\"Bind this model to its host environment and topology context (called by the framework).\"\"\"\n    self.sink = sink\n    self.neighbors = neighbors\n    self.id = node_id\n</code></pre>"},{"location":"api/model/#risansym.Model.set_time","title":"<code>set_time(time)</code>","text":"<p>Advance the node's local clock (called by the framework).</p> Source code in <code>core/src/risansym/model.py</code> <pre><code>def set_time(self, time: float) -&gt; None:\n    \"\"\"Advance the node's local clock (called by the framework).\"\"\"\n    self.clock = time\n</code></pre>"},{"location":"api/model/#risansym.Model.transmit","title":"<code>transmit(event)</code>","text":"<p>Schedule a message transmission to another node.</p> Source code in <code>core/src/risansym/model.py</code> <pre><code>def transmit(self, event: Event) -&gt; None:\n    \"\"\"Schedule a message transmission to another node.\"\"\"\n    if self.sink is None:\n        raise RuntimeError(\n            f\"Model(id={self.id}) cannot transmit: not bound to a Process. \"\n            \"Ensure Simulation.initialize_all() has been called.\"\n        )\n    self.sink.transmit(event)\n</code></pre>"},{"location":"api/process/","title":"Process","text":""},{"location":"api/process/#risansym.Process","title":"<code>risansym.Process</code>","text":"<p>Entity that resides at a vertex of the topology graph.</p> <p>A process hosts exactly one :class:<code>Model</code> and mediates communication between the model's algorithm logic and the simulation engine.</p> Source code in <code>core/src/risansym/process.py</code> <pre><code>class Process:\n    \"\"\"Entity that resides at a vertex of the topology graph.\n\n    A process hosts exactly one :class:`Model` and mediates communication\n    between the model's algorithm logic and the simulation engine.\n    \"\"\"\n\n    def __init__(self, neighbors: list[int], engine: Simulator, node_id: int) -&gt; None:\n        self.neighbors = neighbors\n        self.engine = engine\n        self.id = node_id\n        self.model: Model | None = None\n\n    def __repr__(self) -&gt; str:\n        return f\"&lt;Process(id={self.id}, neighbors={self.neighbors})&gt;\"\n\n    def set_model(self, model: Model) -&gt; None:\n        \"\"\"Bind a model to this process without triggering initialization.\"\"\"\n        self.model = model\n        self.model.set_sink(self, self.neighbors, self.id)\n\n    def set_time(self, time: float) -&gt; None:\n        \"\"\"Forward the simulation clock to the bound model.\"\"\"\n        if self.model:\n            self.model.set_time(time)\n\n    def transmit(self, event: Event) -&gt; None:\n        \"\"\"Delegate event insertion to the engine, attaching the node's current state.\"\"\"\n        state = self.model.get_state() if self.model else {}\n        self.engine.insert_event(event, node_state=state)\n\n    def receive(self, event: Event) -&gt; None:\n        \"\"\"Deliver an incoming event to the bound model for processing.\"\"\"\n        if self.model:\n            self.model.receive(event)\n\n    def log(self, message: str) -&gt; None:\n        \"\"\"Record an application-level log event via the engine.\"\"\"\n        self.engine.log_app_event(self.id, message)\n</code></pre>"},{"location":"api/process/#risansym.Process.log","title":"<code>log(message)</code>","text":"<p>Record an application-level log event via the engine.</p> Source code in <code>core/src/risansym/process.py</code> <pre><code>def log(self, message: str) -&gt; None:\n    \"\"\"Record an application-level log event via the engine.\"\"\"\n    self.engine.log_app_event(self.id, message)\n</code></pre>"},{"location":"api/process/#risansym.Process.receive","title":"<code>receive(event)</code>","text":"<p>Deliver an incoming event to the bound model for processing.</p> Source code in <code>core/src/risansym/process.py</code> <pre><code>def receive(self, event: Event) -&gt; None:\n    \"\"\"Deliver an incoming event to the bound model for processing.\"\"\"\n    if self.model:\n        self.model.receive(event)\n</code></pre>"},{"location":"api/process/#risansym.Process.set_model","title":"<code>set_model(model)</code>","text":"<p>Bind a model to this process without triggering initialization.</p> Source code in <code>core/src/risansym/process.py</code> <pre><code>def set_model(self, model: Model) -&gt; None:\n    \"\"\"Bind a model to this process without triggering initialization.\"\"\"\n    self.model = model\n    self.model.set_sink(self, self.neighbors, self.id)\n</code></pre>"},{"location":"api/process/#risansym.Process.set_time","title":"<code>set_time(time)</code>","text":"<p>Forward the simulation clock to the bound model.</p> Source code in <code>core/src/risansym/process.py</code> <pre><code>def set_time(self, time: float) -&gt; None:\n    \"\"\"Forward the simulation clock to the bound model.\"\"\"\n    if self.model:\n        self.model.set_time(time)\n</code></pre>"},{"location":"api/process/#risansym.Process.transmit","title":"<code>transmit(event)</code>","text":"<p>Delegate event insertion to the engine, attaching the node's current state.</p> Source code in <code>core/src/risansym/process.py</code> <pre><code>def transmit(self, event: Event) -&gt; None:\n    \"\"\"Delegate event insertion to the engine, attaching the node's current state.\"\"\"\n    state = self.model.get_state() if self.model else {}\n    self.engine.insert_event(event, node_state=state)\n</code></pre>"},{"location":"api/schemas/","title":"Schemas","text":""},{"location":"api/schemas/#risansym.schemas","title":"<code>risansym.schemas</code>","text":""},{"location":"api/schemas/#risansym.schemas.AppLogEvent","title":"<code>AppLogEvent</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Recorded when a node emits an application-level log message.</p> Source code in <code>core/src/risansym/schemas.py</code> <pre><code>class AppLogEvent(BaseModel):\n    \"\"\"Recorded when a node emits an application-level log message.\"\"\"\n\n    action: Literal[\"APP_LOG\"] = \"APP_LOG\"\n    clock: float\n    source: int\n    message: str\n</code></pre>"},{"location":"api/schemas/#risansym.schemas.ReceiveEvent","title":"<code>ReceiveEvent</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Recorded when a node processes an incoming message.</p> Source code in <code>core/src/risansym/schemas.py</code> <pre><code>class ReceiveEvent(BaseModel):\n    \"\"\"Recorded when a node processes an incoming message.\"\"\"\n\n    action: Literal[\"RECEIVE\"] = \"RECEIVE\"\n    clock: float = Field(..., description=\"Time at which the node processes the event\")\n    source: int\n    target: int\n    name: str\n    payload: Any\n    node_state: dict[str, Any] | None = None\n</code></pre>"},{"location":"api/schemas/#risansym.schemas.TraceMetadata","title":"<code>TraceMetadata</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Metadata attached to a complete simulation trace.</p> Source code in <code>core/src/risansym/schemas.py</code> <pre><code>class TraceMetadata(BaseModel):\n    \"\"\"Metadata attached to a complete simulation trace.\"\"\"\n\n    schema_version: Literal[\"1.0\"] = \"1.0\"\n    algorithm: str\n    topology: str\n    tag: str | None = None\n    execution_date: str\n    parameters: dict[str, Any]\n    metrics: dict[str, Any]\n</code></pre>"},{"location":"api/schemas/#risansym.schemas.TraceOutput","title":"<code>TraceOutput</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Top-level container for a simulation trace file.</p> Source code in <code>core/src/risansym/schemas.py</code> <pre><code>class TraceOutput(BaseModel):\n    \"\"\"Top-level container for a simulation trace file.\"\"\"\n\n    metadata: TraceMetadata\n    trace: list[TraceEvent]\n</code></pre>"},{"location":"api/schemas/#risansym.schemas.TransmitEvent","title":"<code>TransmitEvent</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Recorded when a node schedules a message for transmission.</p> Source code in <code>core/src/risansym/schemas.py</code> <pre><code>class TransmitEvent(BaseModel):\n    \"\"\"Recorded when a node schedules a message for transmission.\"\"\"\n\n    action: Literal[\"TRANSMIT\"] = \"TRANSMIT\"\n    clock: float = Field(..., description=\"Time at which the sender dispatched the event\")\n    event_time: float = Field(..., description=\"Computed arrival time at the target node\")\n    source: int\n    target: int\n    name: str\n    payload: Any\n    node_state: dict[str, Any] | None = None\n</code></pre>"},{"location":"api/simulation/","title":"Simulation","text":""},{"location":"api/simulation/#risansym.Simulation","title":"<code>risansym.Simulation</code>","text":"<p>Global orchestrator for the computational graph and simulation cycle.</p> <p>Creates processes, binds algorithm models, and drives the event loop until completion.</p> <p>Use the <code>from_file</code> classmethod to instantiate directly from a topology file.</p> <p>Parameters:</p> Name Type Description Default <code>graph</code> <code>list[list[int]] | str | Path</code> <p>The adjacency-list representing the topology graph.</p> required <code>maxtime</code> <code>float</code> <p>Maximum simulation time horizon.</p> required <code>algo_name</code> <code>str</code> <p>Human-readable algorithm identifier for trace metadata.</p> <code>'UnknownAlgo'</code> <code>debug</code> <code>bool</code> <p>If <code>True</code>, print event-by-event debug output to stdout.</p> <code>True</code> <code>trace</code> <code>bool | str | Path</code> <p>Controls trace output \u2014 <code>False</code> disables tracing, <code>True</code> auto-generates a file path, or a <code>str</code>/<code>Path</code> sets the output path.</p> <code>False</code> <code>trace_dir</code> <code>str</code> <p>Base directory for auto-generated trace files.</p> <code>'traces'</code> <code>trace_tag</code> <code>str | None</code> <p>Optional tag appended to the auto-generated filename.</p> <code>None</code> Source code in <code>core/src/risansym/simulation.py</code> <pre><code>class Simulation:\n    \"\"\"Global orchestrator for the computational graph and simulation cycle.\n\n    Creates processes, binds algorithm models, and drives the event loop until completion.\n\n    Use the `from_file` classmethod to instantiate directly from a topology file.\n\n    Args:\n        graph: The adjacency-list representing the topology graph.\n        maxtime: Maximum simulation time horizon.\n        algo_name: Human-readable algorithm identifier for trace metadata.\n        debug: If ``True``, print event-by-event debug output to stdout.\n        trace: Controls trace output \u2014 ``False`` disables tracing, ``True``\n            auto-generates a file path, or a ``str``/``Path`` sets the output path.\n        trace_dir: Base directory for auto-generated trace files.\n        trace_tag: Optional tag appended to the auto-generated filename.\n    \"\"\"\n\n    def __init__(\n        self,\n        graph: list[list[int]] | str | Path,\n        maxtime: float,\n        algo_name: str = \"UnknownAlgo\",\n        debug: bool = True,\n        trace: bool | str | Path = False,\n        trace_dir: str = \"traces\",\n        trace_tag: str | None = None,\n    ) -&gt; None:\n        from risansym.trace import TraceCollector\n\n        self.algo_name = algo_name\n        self.trace = trace\n        self.trace_dir = trace_dir\n        self.trace_tag = trace_tag\n        self._topology_name = \"unknown_topology\"\n        self._initialized = False\n\n        self.collector = TraceCollector() if trace else None\n        self.engine = Simulator(maxtime, debug=debug, collector=self.collector)\n\n        # Backwards compatibility: Duck typing the constructor\n        if isinstance(graph, (str, Path)):\n            warnings.warn(\n                \"Passing a filename directly to Simulation() is deprecated \"\n                \"and will be removed in v1.0. Use Simulation.from_file() instead.\",\n                DeprecationWarning,\n                stacklevel=2,\n            )\n            self.graph = load_adjacency_matrix(graph)\n            self._topology_name = Path(graph).stem\n        else:\n            self.graph = graph\n\n        self.execution_metrics: dict[str, Any] = {}\n\n        # Index 0 reserved as None; nodes are 1-indexed\n        self.table: list[Process | None] = [None] + [\n            Process(row, self.engine, i)\n            for i, row in enumerate(self.graph, start=1)\n        ]\n\n    def __repr__(self) -&gt; str:\n        nodes = len(self.table) - 1\n        return f\"&lt;Simulation(algo='{self.algo_name}', topology='{self._topology_name}', nodes={nodes})&gt;\"\n\n    @classmethod\n    def from_file(\n        cls,\n        filename: str | Path,\n        maxtime: float,\n        algo_name: str = \"UnknownAlgo\",\n        debug: bool = True,\n        trace: bool | str | Path = False,\n        trace_dir: str = \"traces\",\n        trace_tag: str | None = None,\n    ) -&gt; Simulation:\n        \"\"\"Factory method to instantiate a Simulation from a topology file.\"\"\"\n        graph = load_adjacency_matrix(filename)\n        instance = cls(\n            graph=graph,\n            maxtime=maxtime,\n            algo_name=algo_name,\n            debug=debug,\n            trace=trace,\n            trace_dir=trace_dir,\n            trace_tag=trace_tag,\n        )\n        instance._topology_name = Path(filename).stem\n        return instance\n\n\n\n    def set_model(self, model: Model, node_id: int) -&gt; None:\n        \"\"\"Bind an algorithm model to a specific node.\n\n        Raises:\n            IndexError: If ``node_id`` is outside the topology.\n        \"\"\"\n        if node_id &lt; 1 or node_id &gt;= len(self.table):\n            raise IndexError(f\"Node {node_id} does not exist in the topology.\")\n        if process := self.table[node_id]:\n            process.set_model(model)\n\n    def initialize_all(self) -&gt; None:\n        \"\"\"Initialize all bound models.\n\n        Call this **after** all models have been assigned via :meth:`set_model`\n        to ensure the full topology is available when ``Model.init()`` runs.\n        \"\"\"\n        self._initialized = True\n        for process in self.table:\n            if process and process.model:\n                process.model.init()\n\n    def init(self, event: Event) -&gt; None:\n        \"\"\"Manually insert a seed event into the simulation engine.\"\"\"\n        self.engine.insert_event(event)\n\n    def _execute(self) -&gt; None:\n        \"\"\"Main loop: pop and route events until the agenda is empty.\"\"\"\n        start_real_time = time.perf_counter()\n\n        while self.engine.is_on:\n            event = self.engine.pop_event()\n            if process := self.table[event.target]:\n                process.set_time(event.time)\n                process.receive(event)\n\n                # Snapshot the node's internal state AFTER processing\n                state = process.model.get_state() if process.model else {}\n\n                if self.collector:\n                    self.collector.record(ReceiveEvent(\n                        action=\"RECEIVE\",\n                        clock=event.time,\n                        source=event.source,\n                        target=event.target,\n                        name=event.name,\n                        payload=event.payload,\n                        node_state=state\n                    ))\n\n        end_real_time = time.perf_counter()\n\n        self.execution_metrics = {\n            \"simulated_time_elapsed\": self.engine.clock,\n            \"total_messages\": self.collector.get_event_count() if self.collector else 0,\n            \"execution_real_time_sec\": round(end_real_time - start_real_time, 5)\n        }\n\n    def _save_trace(self) -&gt; None:\n        \"\"\"Serialize and persist the trace with metadata.\"\"\"\n        graph_name = self._topology_name\n        tag = f\"_{self.trace_tag}\" if self.trace_tag else \"\"\n        timestamp = datetime.datetime.now(datetime.timezone.utc).strftime(\"%Y%m%d_%H%M%S\")\n\n        if isinstance(self.trace, (str, Path)) and not isinstance(self.trace, bool):\n            trace_path = Path(self.trace)\n        else:\n            file_name = f\"{self.algo_name}_{graph_name}{tag}_{timestamp}.json\"\n            trace_path = Path(self.trace_dir) / self.algo_name / file_name\n\n        total_edges = sum(len(neighbors) for neighbors in self.graph)\n        total_nodes = len(self.table) - 1\n\n        metadata = TraceMetadata(\n            schema_version=\"1.0\",\n            algorithm=self.algo_name,\n            topology=graph_name,\n            tag=self.trace_tag,\n            execution_date=timestamp,\n            parameters={\n                \"max_time\": self.engine.maxtime,\n                \"total_nodes\": total_nodes,\n                \"total_edges\": total_edges\n            },\n            metrics=self.execution_metrics\n        )\n\n        if self.collector:\n            self.collector.dump(trace_path, metadata)\n\n    def run(self) -&gt; None:\n        \"\"\"Entry point: execute the simulation and optionally save the trace.\"\"\"\n        if not self._initialized:\n            warnings.warn(\n                \"Calling run() without calling initialize_all() is deprecated. \"\n                \"Models were auto-initialized, but you must do this explicitly in v1.0.\",\n                DeprecationWarning,\n                stacklevel=2,\n            )\n            self.initialize_all()\n\n        self._execute()\n        if self.trace:\n            self._save_trace()\n</code></pre>"},{"location":"api/simulation/#risansym.Simulation.from_file","title":"<code>from_file(filename, maxtime, algo_name='UnknownAlgo', debug=True, trace=False, trace_dir='traces', trace_tag=None)</code>  <code>classmethod</code>","text":"<p>Factory method to instantiate a Simulation from a topology file.</p> Source code in <code>core/src/risansym/simulation.py</code> <pre><code>@classmethod\ndef from_file(\n    cls,\n    filename: str | Path,\n    maxtime: float,\n    algo_name: str = \"UnknownAlgo\",\n    debug: bool = True,\n    trace: bool | str | Path = False,\n    trace_dir: str = \"traces\",\n    trace_tag: str | None = None,\n) -&gt; Simulation:\n    \"\"\"Factory method to instantiate a Simulation from a topology file.\"\"\"\n    graph = load_adjacency_matrix(filename)\n    instance = cls(\n        graph=graph,\n        maxtime=maxtime,\n        algo_name=algo_name,\n        debug=debug,\n        trace=trace,\n        trace_dir=trace_dir,\n        trace_tag=trace_tag,\n    )\n    instance._topology_name = Path(filename).stem\n    return instance\n</code></pre>"},{"location":"api/simulation/#risansym.Simulation.init","title":"<code>init(event)</code>","text":"<p>Manually insert a seed event into the simulation engine.</p> Source code in <code>core/src/risansym/simulation.py</code> <pre><code>def init(self, event: Event) -&gt; None:\n    \"\"\"Manually insert a seed event into the simulation engine.\"\"\"\n    self.engine.insert_event(event)\n</code></pre>"},{"location":"api/simulation/#risansym.Simulation.initialize_all","title":"<code>initialize_all()</code>","text":"<p>Initialize all bound models.</p> <p>Call this after all models have been assigned via :meth:<code>set_model</code> to ensure the full topology is available when <code>Model.init()</code> runs.</p> Source code in <code>core/src/risansym/simulation.py</code> <pre><code>def initialize_all(self) -&gt; None:\n    \"\"\"Initialize all bound models.\n\n    Call this **after** all models have been assigned via :meth:`set_model`\n    to ensure the full topology is available when ``Model.init()`` runs.\n    \"\"\"\n    self._initialized = True\n    for process in self.table:\n        if process and process.model:\n            process.model.init()\n</code></pre>"},{"location":"api/simulation/#risansym.Simulation.run","title":"<code>run()</code>","text":"<p>Entry point: execute the simulation and optionally save the trace.</p> Source code in <code>core/src/risansym/simulation.py</code> <pre><code>def run(self) -&gt; None:\n    \"\"\"Entry point: execute the simulation and optionally save the trace.\"\"\"\n    if not self._initialized:\n        warnings.warn(\n            \"Calling run() without calling initialize_all() is deprecated. \"\n            \"Models were auto-initialized, but you must do this explicitly in v1.0.\",\n            DeprecationWarning,\n            stacklevel=2,\n        )\n        self.initialize_all()\n\n    self._execute()\n    if self.trace:\n        self._save_trace()\n</code></pre>"},{"location":"api/simulation/#risansym.Simulation.set_model","title":"<code>set_model(model, node_id)</code>","text":"<p>Bind an algorithm model to a specific node.</p> <p>Raises:</p> Type Description <code>IndexError</code> <p>If <code>node_id</code> is outside the topology.</p> Source code in <code>core/src/risansym/simulation.py</code> <pre><code>def set_model(self, model: Model, node_id: int) -&gt; None:\n    \"\"\"Bind an algorithm model to a specific node.\n\n    Raises:\n        IndexError: If ``node_id`` is outside the topology.\n    \"\"\"\n    if node_id &lt; 1 or node_id &gt;= len(self.table):\n        raise IndexError(f\"Node {node_id} does not exist in the topology.\")\n    if process := self.table[node_id]:\n        process.set_model(model)\n</code></pre>"},{"location":"tutorial/getting_started/","title":"Getting Started","text":"<p>Learn how to install Risansym and set up your first simulation.</p>"},{"location":"tutorial/getting_started/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.10 or higher.</li> <li>(Optional but recommended) <code>uv</code> for lightning-fast package management.</li> </ul>"},{"location":"tutorial/getting_started/#installation","title":"Installation","text":"<p>Install the simulation core directly from PyPI:</p> <pre><code>uv add risansym\n# or using pip\npip install risansym\n</code></pre>"},{"location":"tutorial/getting_started/#creating-a-topology","title":"Creating a Topology","text":"<p>Risansym represents network topologies using standard adjacency-list text files. Create a file named <code>graph.txt</code>:</p> <pre><code># node: neighbors\n1: 2\n2: 1 3\n3: 2\n</code></pre>"},{"location":"tutorial/getting_started/#running-the-simulation","title":"Running the Simulation","text":"<p>You can instantiate the simulator by passing your topology file.</p> <pre><code>from risansym import Simulation\n\n# Create the simulation engine\nengine = Simulation.from_file(\n    filename=\"graph.txt\",\n    maxtime=100.0,\n    algo_name=\"MyFirstAlgorithm\",\n    debug=True,\n    trace=True\n)\n\n# Initialize all models (we haven't attached any yet!)\nengine.initialize_all()\n\n# Run the simulation loop\nengine.run()\n</code></pre> <p>If <code>trace=True</code> is set, Risansym will automatically generate a detailed JSON file in the <code>traces/</code> folder when the simulation finishes. This file can be uploaded to the Web Visualizer.</p> <p>Proceed to Writing Algorithms to learn how to inject behavior into your nodes.</p>"},{"location":"tutorial/using_visualizer/","title":"Using the Visualizer","text":"<p>Risansym features a beautiful, React-based web application designed to parse and animate the JSON traces generated by the Python core.</p>"},{"location":"tutorial/using_visualizer/#running-the-web-app","title":"Running the Web App","text":"<p>Currently, the visualizer is a Next.js application located in the <code>web/</code> folder of the repository. You can run it locally:</p> <pre><code>cd web\nnpm install\nnpm run dev\n</code></pre> <p>Navigate to <code>http://localhost:3000</code> in your browser.</p>"},{"location":"tutorial/using_visualizer/#uploading-traces","title":"Uploading Traces","text":"<p>When you run a simulation in Python with <code>trace=True</code>, a JSON file is saved in the <code>traces/</code> folder. Simply drag and drop this JSON file into the Web Visualizer's drop zone.</p>"},{"location":"tutorial/using_visualizer/#features","title":"Features","text":"<ul> <li>Timeline Playback: Play, pause, fast-forward, and rewind the entire simulation event by event.</li> <li>De Casteljau Animations: Message transmissions are dynamically rendered as beautiful Bezier curves connecting nodes, animated perfectly based on mathematical time curves.</li> <li>State Inspection: Click on any event node (the geometric dots on the lifelines) to open a floating panel containing the event's raw JSON payload, timing information, and the node's internal state at that exact moment.</li> <li>Zoom &amp; Pan: Fully scalable vector-based (SVG) rendering allows you to zoom in on dense event clusters.</li> </ul>"},{"location":"tutorial/writing_algorithms/","title":"Writing Algorithms","text":"<p>In Risansym, distributed algorithms are defined by creating a class that inherits from <code>Model</code>. A model defines the behavior of a single node in your distributed network.</p>"},{"location":"tutorial/writing_algorithms/#the-model-class","title":"The Model Class","text":"<p>Your custom algorithm must implement two core lifecycle methods:</p> <ol> <li><code>init(self)</code>: Called exactly once when the simulation starts. Use this to initialize state variables or send the first messages.</li> <li><code>receive(self, event: Event)</code>: Called every time the node receives a message from another node.</li> </ol>"},{"location":"tutorial/writing_algorithms/#example-ping-pong-protocol","title":"Example: Ping-Pong Protocol","text":"<p>Let's build a simple algorithm where Node 1 sends a \"PING\" to Node 2, and Node 2 replies with a \"PONG\".</p> <pre><code>from risansym import Model, Event, Simulation\n\nclass PingPong(Model):\n    def init(self):\n        # We start the protocol if we are node 1\n        if self.id == 1:\n            # We assume node 2 is our neighbor\n            target = self.neighbors[0]\n            # Send the PING event to arrive at time = clock + 1.0\n            self.transmit(Event(\n                time=self.clock + 1.0, \n                name=\"PING\", \n                target=target, \n                source=self.id\n            ))\n            self.log(f\"Started ping to node {target}\")\n\n    def receive(self, event: Event):\n        self.log(f\"Received {event.name} from node {event.source}\")\n\n        # If we receive PING, reply with PONG\n        if event.name == \"PING\":\n            self.transmit(Event(\n                time=self.clock + 1.0,\n                name=\"PONG\",\n                target=event.source,\n                source=self.id\n            ))\n</code></pre>"},{"location":"tutorial/writing_algorithms/#attaching-the-algorithm","title":"Attaching the Algorithm","text":"<p>Once you define your class, you bind instances of it to the simulation nodes:</p> <pre><code># Assuming graph.txt has at least 2 nodes\nsim = Simulation.from_file(\"graph.txt\", maxtime=10.0, debug=True)\n\n# Bind a fresh instance of PingPong to every node in the graph\nfor i in range(1, len(sim.graph) + 1):\n    sim.set_model(PingPong(), i)\n\nsim.initialize_all()\nsim.run()\n</code></pre>"},{"location":"tutorial/writing_algorithms/#state-snapshots","title":"State Snapshots","text":"<p>For advanced use cases (and better visualizer data), you can implement <code>get_state()</code> in your model. This method takes a snapshot of your node's internal state after processing every event.</p> <pre><code>class AdvancedModel(Model):\n    def __init__(self):\n        self.messages_processed = 0\n\n    def receive(self, event: Event):\n        self.messages_processed += 1\n\n    def get_state(self) -&gt; dict:\n        return {\n            \"processed\": self.messages_processed\n        }\n</code></pre> <p>This state will be stored inside the generated trace file and displayed in the Web Visualizer when clicking on events.</p>"}]}